if getgenv().foodzik then return end
getgenv().foodzik = true

local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local Credit = Instance.new("TextLabel")
local UIStroke = Instance.new("UIStroke")

ScreenGui.Name = "RbxNayap_Auto_V2"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = 0.5
MainFrame.Position = UDim2.new(0.4, 0, 0.4, 0)
MainFrame.Size = UDim2.new(0, 200, 0, 80)
MainFrame.Active = true
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- Viền Rainbow
UIStroke.Parent = MainFrame
UIStroke.Thickness = 2
UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

task.spawn(function()
    while true do
        local hue = tick() % 5 / 5
        UIStroke.Color = Color3.fromHSV(hue, 1, 1)
        task.wait()
    end
end)

Title.Size = UDim2.new(1, 0, 0.6, 0)
Title.BackgroundTransparency = 1
Title.Text = "GRAB THE FOODZIK!"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 20
Title.Parent = MainFrame

Credit.Size = UDim2.new(1, 0, 0.4, 0)
Credit.Position = UDim2.new(0, 0, 0.5, 0)
Credit.BackgroundTransparency = 1
Credit.Text = "by RbxNayap"
Credit.TextColor3 = Color3.fromRGB(200, 200, 200)
Credit.Font = Enum.Font.SourceSansItalic
Credit.TextSize = 16
Credit.Parent = MainFrame

local BridgeNet = ReplicatedStorage:WaitForChild("ncxyzero_bridgenet2-fork@1.1.5"):WaitForChild("dataRemoteEvent")

-- [AUTO 1] Force Foodzik (Tự động chạy)
task.spawn(function()
    while true do
        local character = Players.LocalPlayer.Character
        if character and not character:FindFirstChild("phone") then
            task.wait(0.1)
            if not character:FindFirstChild("phone") then
                local args = {
                    {
                        {
                            "\001",
                            ")\006\144\196\215\130M\006\178'\149\131\179\181@\226",
                            { workspace:WaitForChild("Bu\197\130as") }
                        },
                        "("
                    }
                }
                BridgeNet:FireServer(unpack(args))
            end
        end
        task.wait(0.5)
    end
end)

-- [AUTO 2] XP Skill Farming (Tự động chạy mỗi 30.1s)
task.spawn(function()
    while true do
        BridgeNet:FireServer({[1]=2, [2]="\b"})
        BridgeNet:FireServer({[1]=1, [2]="\b"})
        task.wait(30.1)
    end
end)
